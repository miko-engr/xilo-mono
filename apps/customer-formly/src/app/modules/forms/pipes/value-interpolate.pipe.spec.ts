import { ValueInterpolatePipe } from './value-interpolate.pipe';

describe('ValueInterpolatePipe', () => {
  it('create an instance', () => {
    const pipe = new ValueInterpolatePipe();
    expect(pipe).toBeTruthy();
  });
});
